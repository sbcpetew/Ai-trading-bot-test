[tool.poetry]
name = "alphafive"
version = "0.1.0"
description = "AlphaFive RL trading bot"
authors = ["AI"]

[tool.poetry.dependencies]
python = ">=3.8"
torch = "*"
gym = "*"
redis = "*"
websockets = "*"
Flask = "*"
python-dotenv = "*"

[tool.poetry.dev-dependencies]
pytest = "*"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
